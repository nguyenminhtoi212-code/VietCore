<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myempty.vietcore">

    <!-- QUYỀN HỆ THỐNG CAO CẤP (NGĂN CHẶN THEO DÕI & CAN THIỆP) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- LÁ CHẮN ÂM THANH & HÌNH ẢNH (ANTI-SPYWARE) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Ngăn chặn can thiệp từ ứng dụng bên thứ 3 vào bộ nhớ -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:node="replace" />

    <!-- LOẠI BỎ ID QUẢNG CÁO & THEO DÕI (DỮ LIỆU SẠCH) -->
    <uses-permission android:name="android.permission.AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    
    <!-- QUYỀN CHẠY TRÊN HỆ THỐNG (PROTECTION LEVEL: SIGNATURE) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_my_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_my_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true"
        android:hasFragileUserData="true"
        android:extractNativeLibs="true"
        tools:targetApi="35">

        <!-- GIẤY PHÉP TỰ ĐỘNG (VIETCORE AUTONOMOUS LICENSE 2026) -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="vietcore.intelligence.LICENSE"
            android:value="AUTONOMOUS_SYSTEM_2026_FREE" />

        <!-- MÀN HÌNH CHỜ (SPLASH): XÁC THỰC GIẤY PHÉP HỆ THỐNG -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- GIAO DIỆN CHÍNH (MAIN): CHẠY TRÊN TIẾN TRÌNH BIỆT LẬP OMNIS_CORE -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:process=":omnis_core"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- DỊCH VỤ ĐỒNG BỘ MÁY CHỦ & BẢO VỆ NHÂN (KERNEL PROTECTION) -->
        <service
            android:name=".SecuritySyncService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <!-- NGĂN CHẶN CHẠY NGẦM GÂY LAG & TỐI ƯU BỘ NHỚ KHI KHỞI ĐỘNG -->
        <receiver
            android:name=".SecurityBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
