<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Mẫu gốc bạn cung cấp -->
    <style name="Base.AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Tùy chỉnh thêm màu sắc hệ thống tại đây nếu cần -->
        <item name="colorPrimary">#00FF41</item>
    </style>

    <style name="AppTheme" parent="Base.AppTheme" />

    <!-- Style dành riêng cho SplashActivity để sửa lỗi FAILURE -->
    <style name="Theme.AppCompat.NoActionBar.FullScreen" parent="Base.AppTheme">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:background">#000000</item>
    </style>
</resources>
